<h1>Admin Dashboard</h1>

<h2>Users</h2>
<ul>
    <% users.forEach(u=> { %>
        <li>
            <%= u.username %> — <%= u.firstName %>
                    <%= u.lastName %>
                        <form action="/admin/delete-user/<%= u._id %>" method="POST" style="display:inline;">
                            <button>Delete</button>
                        </form>
        </li>
        <% }) %>
</ul>

<h2>Posts</h2>
<ul>
    <% posts.forEach(p=> { %>
        <li>
            <%= p.message %> (by <%= p.author %>)
                    <form action="/admin/delete-post/<%= p._id %>" method="POST" style="display:inline;">
                        <button>Delete</button>
                    </form>
        </li>
        <% }) %>
</ul>